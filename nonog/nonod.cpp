#include <iostream>
#include <fstream>
#include <sstream>
#include <stack>
using namespace std;

	/*
	getline(in_file, in_line);	// READ DOLLAR SIGN
	for(int i=0;i<50;i++)	//READ EACH LINE
	{
		getline(in_file, in_line);
		cout << in_line << endl;
		
		ss.str("");
		ss.clear();
		ss << in_line;
		while(ss >> tmp_num)
		{
			tmpstk.push(tmp_num);
			cout << tmpstk.top() << " ";
			tmpstk.pop();
		}
		cout << endl; 
	}
	*/


	/* STACK CHECK
	for(int i=0;i<25;i++)
	{
		
		while(!d_col[i].empty())
		{
			cout << d_col[i].top() << " ";
			d_col[i].pop();
		}
		cout << endl;
		
		//cout << d_col[i].size() << endl;
	}
	
	for(int i=0;i<25;i++)
	{
		
		while(!d_row[i].empty())
		{
			cout << d_row[i].top() << " ";
			d_row[i].pop();
		}
		cout << endl;
		
		//cout << d_row[i].size() << endl;
	}
	*/
	
	
		/*
		while(ss >> tmp_num)
		{
			d_col[i].push(tmp_num);
			tmpstk1.push(tmp_num);
			//cout << tmpstk.top() << " ";
			//tmpstk.pop();
		}
		*/
		/*
		while(!tmpstk1.empty())
		{
			cout << tmpstk1.top() << " ";
			tmpstk1.pop();
		}
		cout << endl;
		*/
		
		
		/*
		while(ss >> tmp_num)
		{
			d_row[i].push(tmp_num);
			tmpstk2.push(tmp_num);
			//cout << tmpstk.top() << " ";
			//tmpstk.pop();
		}
		*/
		/* 
		while(!tmpstk2.empty())
		{
			cout << tmpstk2.top() << " ";
			tmpstk2.pop();
		}
		cout << endl;
		*/
